<h1>All Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <strong><%= product.title %></strong> —
      $<%= product.price %> —
      Stock: <%= product.stock_quantity %>
    </li>
  <% end %>
</ul>